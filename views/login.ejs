<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Login - Husn Hira</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/styles/login.css">
</head>
<body>
  <div class="container">
    <!-- Brand Panel -->
    <div class="brand-panel">
      <div class="logo-container">
        <div class="logo-placeholder">
          <i class="fas fa-lock" style="font-size: 60px; margin-bottom: 20px;"></i>
        </div>
      </div>
      <h1>Husn Hira<br>Admin Portal</h1>
      <p>Secure access to your administration dashboard. Manage your platform with confidence.</p>
    </div>

    <!-- Login Form -->
    <div class="login-box">
      <h2>Admin Login</h2>

      <div class="error" style="display: none;">
        <i class="fas fa-exclamation-circle"></i> Incorrect username or password
      </div>

      <form id="loginForm" action="/admin-portal-1024/login-secret" method="POST">
        <div class="input-group">
          <label for="username">Username</label>
          <input type="text" id="username" name="username" placeholder="Enter your username" required>
          <i class="fas fa-user"></i>
        </div>
        <div class="input-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" placeholder="Enter your password" required>
          <i class="fas fa-lock"></i>
        </div>
        <button type="submit" id="loginBtn">Login</button>
      </form>
      
      <div class="login-footer">
        <p>Having trouble? <a href="#">Contact Support</a></p>
        <p>© 2023 Husn Hira Admin Portal. All rights reserved.</p>
      </div>
    </div>
  </div>
  
 <script>
  document.getElementById('loginForm').addEventListener('submit', function(e) {
    e.preventDefault();

    const loginBtn = document.getElementById('loginBtn');

    loginBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Authenticating...';
    loginBtn.classList.add('logging-in');
    loginBtn.disabled = true;

    // Slight delay before real submission (for UI effect only)
    setTimeout(() => {
      this.submit();
    }, 800);
  });
</script>

</body>
</html>